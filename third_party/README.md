# Third-party setup

This directory is intentionally ignored by Git. Install the pinned upstream
repositories described in the root [README.md](../README.md) before running
the MuJoCo prototype or GPU workers. Do not place checkpoints, model weights,
credentials, or generated assets in this repository.
