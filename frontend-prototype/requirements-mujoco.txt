# Runtime aligned with third_party/mujoco-main (CMake source: 3.12.1).
# PyPI currently provides the compatible Windows wheel as 3.12.0.
numpy==1.26.4
mujoco==3.12.0

# Optional: only needed when a .pt file contains a qpos/action tensor.
# Policy-only checkpoints remain visible in the asset library without torch.
# torch==2.5.1
